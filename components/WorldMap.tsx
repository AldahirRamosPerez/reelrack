export default function WorldMap({ data }: { data: any }) {
  return (
    <div className="h-[500px] bg-gray-100 dark:bg-gray-800 rounded-lg flex items-center justify-center">
      <p>Mapa de países (próximamente)</p>
    </div>
  );
}